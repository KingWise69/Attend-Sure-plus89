// Function to handle the navigation (similar to React Native navigation)
function navigateToLogin(role) {
    if (role === 'student') {
        // Replace with actual navigation logic
        window.location.href = "loginp.html"; // Navigate to the Student Login page
    } else if (role === 'teacher') {
        // Replace with actual navigation logic
        window.location.href = "logint.html"; // Navigate to the Teacher Login page
    }
}
